#include "linear_solver.h"
#include <stdio.h>
#include <stdlib.h>
#include "substitution.h"
#include <math.h>

int main(int argc, const char* args[]) {
	
	int precision;
	
	printf("Enter the amount of precision: \n");
	
	scanf("%d", &precision);
	
	printf("Enter the decimal fraction: \n");
	
	scanf(%)
}


void convertFractionToBinary(double fraction, char *digits, int precision) {
	int i = 0;
	
	while (i < precision && fabs(fraction) > .0000001) {
		
	}
}